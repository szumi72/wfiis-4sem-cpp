#include "ScoreSystem.h"

void ScoreSystem::handleEvent(const Event& event){
    // if(dynamic_cast<const DamageEvent*> (&event)){
    //     std::cout<<"[Score system a dynamic cast] "<<event<<std::endl;    
    // }
    std::cout<<"[Score system] "<<event<<std::endl;
}